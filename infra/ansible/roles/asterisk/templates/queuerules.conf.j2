; {{ ansible_managed }}
; Regras de penalidade das filas. Vazio de propósito: as penalidades são
; fixas, definidas por agente na tela de filas. O arquivo existe só para
; o app_queue não avisar a cada reload.
[general]
